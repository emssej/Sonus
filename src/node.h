#pragma once

typedef enum
WidgetType
  {
	 WIDGETTYPE_
  };

typedef struct
Widget
{
  
} Widget;

typedef struct
Node
{
  long x, y;
  const char *title;
  size_t widgets_size;
  Widget *widgets;
} Node;
